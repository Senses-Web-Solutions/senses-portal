// This is not a file you should use
// Just a reference to methods a stacked
// interface should implement

const stacked = {
    items: [],
    push(name, data = {}) {},
    pop(amount = 1)  {},
    clear() {},
    findByKey(key) {},
    get current() {},
    get all() {}
}